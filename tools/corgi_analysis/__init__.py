"""
CORGI analysis shared library.

Modules
-------
bag_loader   — load ROS2 SQLite bags (inner EKF, outer fusion)
vicon_loader — load VICON Nexus CSV (ground plane, robot frame, trigger)
metrics      — rmse, bias_stats, contact_metrics, ekf_metrics
plots        — standard figure helpers
"""
